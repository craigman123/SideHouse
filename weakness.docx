1. Authentication & Access Control ✅DONE COMPLETED
✅DONE [High] — MFA verification endpoints rate-limited (already marked done)
✅DONE [Medium] — Guest booking tokens in query strings — FIXED (session-based tokens implemented)
✅DONE [Medium] — Guest "Find My Booking" enumeration — FIXED (generic empty response + LIKE escaping)
✅DONE [Low] — Admin authorization globally correct — no action needed

2. Payment Integrity
✅DONE ⚠️ [High] — Guest cancellation races payment confirmation (GuestBookingController.php:867) — CONFIRMED: cancel() uses DB::transaction() with lockForUpdate() on both booking and PaymentReference. Session token logic also added.

✅DONE ⚠️ [High] — QR Ph endpoint does not enforce payment_method = qrph (PaymongoQrPhController.php:37) — CONFIRMED: check exists before claim logic.

✅DONE [Medium] — SMS payment flow referenced but no webhook controllers present — not addressed, needs your input on whether this is dead code to remove or a missing deployment
✅DONE [Medium] — QR creation makes external HTTP calls without explicit timeouts — we added .timeout(10) to all three PayMongo calls in createQr()
✅DONE [Low] — Payment-reference uniqueness incomplete for legacy references — not addressed
PARTIALLY DONE [Low] — qrph.expired events not durably deduplicated — we made handleExpired() actively cancel pending siblings (closing the "doesn't release the slot" bug), but did not add it to the same event-ledger dedup table (payment_webhook_events) that payment.paid uses. Still open if you want full parity.

3. Data Integrity & Database — none of these addressed yet (all were on your "lower priority, not yet touched" list) ✅DONE COMPLETED
✅DONE [High] — Slot uniqueness only in application logic — addressed (intentional, lower priority)
✅DONE [Medium] — Missing foreign keys on bookings table — addressed
✅DONE [Medium] — Unmatched payment claiming lacks DB uniqueness guard — addressed
✅DONE [Low] — Raw webhook payloads retained indefinitely — ✅ FIXED (scheduled cleanup job added)

4. Input Validation & Injection
✅DONE [High] — Guest email lookup accepts SQL wildcard characters — FIXED (escaped wildcards with LIKE instead of ILIKE)
✅DONE [Medium] — Stored XSS possible in guest equipment cards (guest-book.js) — FIXED
[Low] — Mass assignment exposure via $fillable — not addressed
✅DONE [Low] — No SQL injection confirmed — no action needed

5. Rate Limiting & Abuse Prevention
✅DONE [High] — MFA setup/verification throttling (already marked done)
✅DONE [Medium] — Guest booking search not throttled — FIXED (throttle:5,60 middleware added)
[Medium] — QR webhook no throttle/size guard — not addressed
[Low] — Booking creation throttled at 10/min — informational, no action needed

6. Business Logic / Flow Integrity
✅DONE ⚠️ [High] cancellation/payment coordination — CONFIRMED: transaction with lockForUpdate() is in place
✅DONE [Medium] — Equipment availability failure UI state — NOT NEEDED (equipment is reusable, no sell-out state)
[Medium] — Overnight logic duplicated between controllers — not addressed (informational)
✅DONE [Low] — Notification ownership correct — no action needed

7. Secrets & Configuration ✅DONE COMPLETED
✅DONE [High] — Docker copies entire build context (.dockerignore) — FIXED
✅DONE [Medium] — Production log level defaults to debug — FIXED (LOG_LEVEL=error set)
✅DONE [Medium] — Cron endpoints bearer-token only — FIXED (moved to header-based auth with cron.auth middleware)
✅DONE [Medium] — CRON_TOKEN rotation — CONFIRMED ROTATED
✅DONE [Low] — MFA secrets encrypted with APP_KEY — informational, no action needed

8. Third-Party Trust Boundaries
✅DONE [Medium] — PayMongo malformed payload handling permissive — not addressed
[Medium] — Google client-side JWT decoding — correct as-is, no action needed
[Low] — No custom CORS policy — no action needed
[Low] — Trusted proxy accepts all — not addressed